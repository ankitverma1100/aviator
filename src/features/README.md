# Features

Feature modules should live here.

Recommended structure per feature:

- `ui/` for React components
- `hooks/` for feature hooks
- `services/` for API/socket adapters
- `types/` for feature-specific types

Existing code is being migrated incrementally to avoid runtime breakage.

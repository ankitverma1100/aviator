# Legacy

Legacy compatibility area for pre-refactor structure.

Current examples in codebase:

- duplicate component trees with casing differences
- older component variants kept for compatibility

New development should target `src/features` and `src/shared`.

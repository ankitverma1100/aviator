# Shared

Shared cross-feature modules:

- `config/` typed runtime configuration
- `utils/` generic helpers
- `types/` shared contracts

Use shared modules only when code is reused by multiple features.
